import './Header.scss';

const Header = () => {

    return (
        <header className="site-header">
            <h1 className="site-header__logo">Bobs Burgers</h1>
        </header>
     );
}

export default Header;
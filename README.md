# Bobs burgers character directory

Using an external API to fetch data, a dynamic list of characters is created.

Should be a case of just using `npm start` to get the project going.
[
Live link can be found here](https://bobs-burgers-stickerbook.vercel.app/);
